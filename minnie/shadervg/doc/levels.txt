* level 5
   * SVG converter script. generates .mib stream / binary file
      * level 4
         * Minnie MIB parser. calls Minnie API which creates MinnieDrawable (draw-list + vertex buffer)
            * level 3
               * MinnieDrawable 
                  * parses draw-list and calls lowlevel rendering functions
                     * level 2
                        * low level rendering functions
                            * call GL driver
                               * level 1
                                  * GL driver. calls low-level driver and creates DaveNX command lists
                                     * level 0
                                        * DaveNX hardware. Parses command lists and sets up HW registers


      
