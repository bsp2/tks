
// $ ls -1 *.png | tks gen_readme.tks >README.md

while(!StdInStream.eof())
{
   String s; StdInStream.readLine(s, 256);
   if(!s.isBlank())
   {
      print "- "+(s.replace(".png",""))+"\n";
      print "  \n";
      print "  ![screenhots]("+s+")\n";
   }
   else
   {
      break;
   }
}
