% apiref test document
% bsp
% 20-Aug-2026
>>>
20Aug2026
<<<

$(var:header)

# Table of contents

$(buf!toc)

# test.h

This is a test for the apiref "test" module.

`apiref` scans the functions (and function pointer) definitions in a C header file and converts them to markdown documentation.

See [test.h](test.h) for the example header source.

$(inc:autogen__apiref.md)


$(buf!footnote)
$(buf!w3validator)

Document created in $(var:gen_ms) on $(var:localdatetime)
].create
