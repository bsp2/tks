
in float i_vel;

out Vector4f o_color;

param float decay def=0.98 min=0 max=1

global float g_value;

if(i_vel > 0.0)
   g_value = 1.0;

g_value *= mathPowerf($(DECAY), g_dt);

o_color.init(g_value, g_value, g_value, 1.0);

