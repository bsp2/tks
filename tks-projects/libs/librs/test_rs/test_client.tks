// 17Aug2024, 18Aug2024, 22Aug2024, 23Aug2024

RemoteScriptClient client;
if( (Arguments & "--debug") || (Arguments & "-d") )
   client.setEnableDebug(true);
if( (Arguments & "--sigusr1") || (Arguments & "-s") )
   client.setEnableSIGUSR1(true);
if(client.initRemoteScriptClient("test_host", 0/*reqSz=def*/))
{
   Integer reqIdx = 0;
   for(;;)
   {
      trace "~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~";
      String *line; int lineNr = 1;
      foreach line in client.evalScript("trace 'hello, world.';").splitChar('\n')
         trace "  host reply:"+(lineNr++)+": "+line;
      // TKS.sleep(1);
     Thread.Yield();
   }
   client.exit();
}
