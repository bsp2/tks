
// ported from "c" to tkscript by bs@tkscript.de
// this was taken from some "c" coding contest 
// (cannot remember the URL right now)

int p=1,i=0;
function printp { print p; } // "print" cannot be used as a statement
for(p=1;++p<100000;
    _=(i>p?printp():0))
   for(i=2;p%i++;);
