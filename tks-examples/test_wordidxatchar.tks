
// String s = "cmd 1 2 3 hello test abc defg";
// String s = "cmd 1 2 3 hello test 'hello, world' abc defg";
String s = "cmd  1 2 3 hello test \"hello, world\" abc defg";
// String s = "'hello, world'";
// String s = "\"hello, world\"";
int idx;

// idx = 1;
// trace "wordidx at charidx "+idx+" is "+s.wordIndexAtCharIndex(idx, true/*bQuot*/);

idx = 0;
loop(s.length)
{
   trace "wordidx at charidx "+idx+" is "+s.wordIndexAtCharIndex(idx, true/*bQuot*/);
   idx++;
}
