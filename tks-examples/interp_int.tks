int i;
int j;
int k;

int t = milliSeconds();

loop(1000000)
{
    k=i*3+i*2+(j+i)*(i*i+j*j)+(j-i)*10;
}

trace "t="+(milliSeconds()-t);


t=milliSeconds();
compile loop(1000000)
{
    k=i*3+i*2+(j+i)*(i*i+j*j)+(j-i)*10;
}
trace "t="+(milliSeconds()-t); // 25 times faster than interp mode.....

