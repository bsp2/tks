
String sLIB = "mylib.tsl";

function main() {
   trace "main()";

   if(TKS.loadLocalTSL(sLIB))
   {
      trace "loadLocalTSL(\""+sLIB+"\") OK";
   }
   else
   {
      trace "[---] failed to load \""+sLIB+"\"";
   }
}
