
function tanh_approx(float f) {
   // f=+-10 => +-1.37
   float ff = f*f;
   return f * (27.0f + ff) / (27.0 + 9.0f*ff);
}

float x = -10;
while(x < 10)
{
   trace "x="+x+" tanh="+tanh(x)+" tanh_approx="+tanh_approx(x);
   x += 0.01;
}
